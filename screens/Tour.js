import React from 'react';
import { StyleSheet, Text, View, Image, TouchableOpacity } from 'react-native';  

const Tour = ({
    params,
}) => (
    <View>
        <Text>Tour Page</Text>
    </View>
);

export default Tour;